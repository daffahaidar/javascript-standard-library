// big integer is a big number library for javascript that can be used to perform mathematical operations on large numbers.

const a = BigInt(Number.MAX_SAFE_INTEGER);
const b = BigInt(Number.MAX_SAFE_INTEGER);

const c = a + b;
console.info(c);
console.info(typeof c);
