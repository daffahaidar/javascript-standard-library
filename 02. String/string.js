const person = "Daffa Haidar Nabil Zufar";

console.info(person.length); // to get the length of the string
console.info(person.toLowerCase()); // to get the string in lowercase
console.info(person.toUpperCase()); // to get the string in uppercase
console.info(person.split(" ")); // to get the string in array format
console.info(person); // to get the string in original format

const input = "     Daffa Haidar Nabil Zufar     ";
console.info(input.trim()); // to get the string after trimming the spaces
console.info(input);
